// react
const { Component } = React;

// react-dom
const { Router, BrowserRouter, HashRouter, Link, NavLink, Switch, Route, Redirect } = ReactRouterDOM;

// redux
const { Provider, connect } = ReactRedux;
const { createStore, combineReducers } = Redux;
